import styled from "styled-components";

const Container = styled.div``;
const Main = styled.main``;
const Footer = styled.footer``;
const Wrapper = styled.div``;
export { Container, Wrapper, Main, Footer };
